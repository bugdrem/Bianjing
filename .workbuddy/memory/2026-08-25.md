# 2026-08-25 工作日志

## 玩法评审（设计师视角，基于 specs + 代码走查）
- 核心循环已跑通：规划→建造→吸引→自生长→征税→晋级；涌现之城（ZoneGrowthSystem）是亮点。
- **最大设计短板：循环无终点、无失败压力、无目标钩子**（无胜负、无危机、无任务）。当前像会自生长的沙盒模型而非游戏。
- 经济货币闭环基本健康；隐患：① 月俸8000/月平白给、早期无张力；② 官粮(gs.Food)不进 DemandLedger，两套粮食口径易出隐性 bug。
- 四类半成品（桩）：互市/特产（ForeignVisitor 进出口未接真实供需）、朝廷赏赐（未接线）、Health（零逻辑）、富户退休闲逛（TODO）。
- 优先级建议（已写入 .workbuddy/artifacts/玩法评审与建议.md）：
  - P0 成败钩子：胜利结算(京城400人+兴致+不欠债) + 危机(饥荒/瘟疫/流寇) + 朝廷任务链。
  - P1 接上已建半成品（互市供需闭环、赏赐→任务、富户消费出口）。
  - P2 经济再平衡(tuning 标 PLACEHOLDER 需 playtest)。
  - P3 表现层(代理动画/索引优化)。
- 待办A（真·毛玻璃面板）根因与修法（重要，已踩坑）：`FrostedPanel` 用 SCREEN_TEXTURE 采样背后画面。Godot 4 里 CanvasLayer 是独立合成 pass，**放在主视口树的 BackBufferCopy 抓到的内容到 HUD 层时已失效**（首版无效）。正确修法：把 `BackBufferCopy{CopyMode=Viewport}` 作为**每个 UI CanvasLayer 的第一个子节点**（Hud/GameMenu/LoadingScreen 的 `_Ready` 开头），先于玻璃面板合成、抓到已画好的世界/天空，面板 SCREEN_TEXTURE 即采到真实背景。`Main.cs` 已移除主视口那两份无效 BackBufferCopy。
- 标题界面背后无世界（未进游戏），菜单玻璃仍会显白：在 `Main._Ready` 调 `SetupTitleBackground()` 给主视口挂天空环境（与游戏内同款暖灰天空），并改 `SetupEnvironment()` 为幂等（_env 已存在则复用，不重复建 WorldEnvironment）。`dotnet build` 0错0警告。

## 待办B 互市供需闭环（本日落地）
- 痛点：ForeignVisitor 的 `SettleVenueTrade` 进出口不读 `gs.Demand`，城市缺/剩什么访客无响应（最大可见空洞）。
- 改动：`MakeCargo` 城市有缺口时 70% 带短缺货补货；`SettleVenueTrade` 出口挑最过剩货从贸易节点(shop/inn/workshop/official)扣库存、城市赚钱，进口仅售城市仍短缺货防积压。
- 调参位置 `VisitorConfig`：ImportBias / ShortageCargoMin/Max / SurplusDaysThreshold / ExportMaxQty / ExportStockShare，全部 [PLACEHOLDER·待 playtest]。
- 失败信号（playtest 前已定义）：A 缺口不带货 / B 已足仍塞货 / C 过剩不收 / D 误扣民居。设计文档见 `.workbuddy/artifacts/互市供需闭环设计.md`。
- 编译 0错0警告。
