# 2026-08-21 工作记录

## 经济系统货币调整（废除黄金，新增万两）
- 修改 `scripts/configs/CurrencyConfig.cs`：删除 `SilverPerGold`/`WenPerGold`（黄金单位），新增 `SilverPerWanLiang=10000`、`WenPerWanLiang=10_000_000`（1 万两 = 10000 两 = 1e7 文）。`WenPerSilver=1000` 不变（千文一两）。
- 修改 `scripts/core/CurrencyHelper.cs`：`FormatWen` 与 `FormatTreasury` 的三级显示由「文/两/金」改为「文/两/万两」（gold 变量 → wanLiang）。`ToSilver`/`FromSilver` 不受影响。
- 同步更新注释与文档：`EconomyConfig.cs` 头部注释、`specs/DESIGN.md` 的 §8.5、§12 注脚、文档顶部「对齐说明」。
- 未动 `CHANGELOG.md`（历史记录，保留黄金作为过往批次事实）；`FormatTreasury` 现为死代码但已一并修正保持一致。
- 全量 grep 确认「金/黄金」货币引用仅存在于上述两文件（其余如 `GoodsColors` 粮金黄、`GridRenderer` 亮金、`Goods` 金工 均为颜色/品类，非货币）。
- `dotnet build` 通过，0 错误 0 警告。
