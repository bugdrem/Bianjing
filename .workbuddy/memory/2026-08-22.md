# 2026-08-22 工作日志

## 阶段 B → C：视觉精修（接续 91/92/93 工作，fix 包子人 + 王爷府 + 院落朝向）

### 阶段 94：包子人 → 宋人比例 + 院落朝向感知

**问题**：
1. 之前给房子开了「亮粉色玻璃盒」(alpha=0.55 + sky color blending) → 改 BodyAlpha 0.92 + AlphaHash
2. 包子人（堆叠的方块）：BoneAttachment3D + Skeleton3D 经多轮试修（BoneIdx/ForceUpdateAllBoneTransforms/ResetBonePoses）都绑不上 → 彻底拆骨架，部件直接挂 _body（已知局限，记入遗留）
3. 人比例 1.45m vs 2m 房高，看着人比房大：ModelScale 0.85 → 0.42（成年高 ≈0.74m，房高 1/3，宋画标准）

### 阶段 95：院落朝向 + 路向感知

**新功能**：
- `FindAdjacentRoadInfo(gs, b, …)` 调 `MapGrid.FindAdjacentRoad` 找最近路格 + `CellAt(r).RoadKind`
- 新规则（用户诉求）：
  - 主/辅路 (Main/Side) → **房子靠路**：房体贴路侧开前门，三面围墙 + 后墙后门
  - 小路 (Lane) → **院子靠路**：房缩到 60% 居中，四面围墙，前墙金门朝路 + 后墙后门
  - 无邻路 → 默认 fallback (0,-1)
- `AddDoorAndWindows` 接 `doorFace` 单位向量，门窗沿墙面切线对称铺
- `AddCourtyardWalls` 接 `roadDir + faceRoadSide`，四面 vs 三面按规则切换
- 抽出 `AddWallSegmentWithDoor` 共用工具（沿 X/Z 两向都支持）

### 遗留（下次迭代）：
- BA3D/Skeleton 骨骼系统已彻底弃用，姿态动画（idle/walk/carry/working）暂时全无 → 阶段 95 后村民站如松走如滑冰
- 画风：纯 BoxMesh + 平面色块永远追不上宋画，下一步候选：
  - **A. 飞檐**（4 旋转 45° 楔形 mesh）
  - **B. 瓦垄着色**（fragment shader sin 波）
  - **C. 真贴图**（美术资源）
- 人物与院子路向感知的规则已落地但需要截图人工验证；预览/相机视角自动朝向当前建筑朝向未实现

**问题索引**（永久保留）：
- dotnet build 偶发卡死/退 1 → `-p:UseSharedCompilation=false`
- 当前阶段不需要做存档兼容（用户明确：阶段 C 早期，别束缚手脚）
- Godot 4.7 代码构造 BoneAttachment3D 路径不可靠 → 拆 BA3D fallback（保留 fallback 路径但默认走 `_body`）
