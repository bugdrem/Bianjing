# 2026-09-01

## 批次九十三收尾：渲染图层拆分完成（本批全部需求闭环）

- `scripts/map/layers/` 十个文件全部落地：`MapLayer`（基类，ChunkCells=64 共享分块划分）、`LayerKit`（VertexNormal/MeshFrom/FillMultiMesh/MakeMulti）、`ChunkGeometry`（MeshBuffers/InstanceBuffers/单块产出容器，TreeModels 按树种分缓冲）、`ChunkGeometryBuilder`（单趟遍历产出水面/三路/桥/树全组缓冲；BuildTrees 轻量只刷树）、六图层 Terrain/Water/Road/Vegetation/Building/Overlay。
- `GridRenderer.cs` 1338 → ~280 行：只留脏标记翻译（MapChanged/CellChanged/RectChanged/BuildingsChanged/TreesChanged）、_Process 预算仲裁（12 块/帧 + 32 块树/帧）、图缘裙板（Scroll 层）；对外 `SetGridVisible`/`SetZonesVisible` 委托 OverlayLayer。`_geo` 单块缓冲复用（Clear/ClearTrees）。
- TerrainLayer 内含顶点色四段（河床/平原橄榄/岩褐 RockRamp 40m/雪线 68m+25m 渐白）与 FitVertex 管线拟合；RockRampMeters 与 MaxTerrainHeight 解耦，主峰 110m 不再整片岩色。
- BuildController 复核：`:400` 拾取下界 −5.5 < 最深湖床 −4.95 ✓；`:396`/`:932` 均随 TerrainConfig 联动 ✓，无需改。
- specs：CHANGELOG 批次九十三、CODEMAP（寻址表 + TerrainConfig/WaterConfig 摘要 + 目录说明）、DESIGN（§4.4 山体重写、§4.5 水系五步汇流管线、§5 图层拆分、配置表、glb 引用改 BuildingLayer.Rebuild）。
- `dotnet build -p:UseSharedCompilation=false` → 0 警告 0 错误。
- 遗留：无编辑器环境，运行时观感（主峰/湖泊/河口/图层）待用户开 Godot 或导出产物验证；老存档建议开新局。
