# 2026-08-28 工作日志

## 全量代码审查（96 .cs / ~21k 行）
- 方法：按子系统分 5 簇并行审查子代理 + 抽验 Critical/High。
- 报告：`/d/Godot/Bianjing/.workbuddy/artifacts/code_review_2026-08-28.md`。
- 严重发现：
  - **C1 Critical**：`SaveService.LoadAsync`(Task.Run)→`LoadData` 后台线程重建道路触发 `EventBus.RaiseRoadReachedEdge`，若 Main 已订阅会跨线程碰 HUD（存档含边缘路必现）。
  - **H5 High**：`ForeignVisitor` 无摊位小贩 `_dwell=float.MaxValue` 永不离开 → 耗尽 `MaxConcurrentVisitors=60` 软锁访客系统。
  - **H1 High**：`Main.EnterWorld` 在 `_inWorld` 提前 return，`_visitors.Setup` 不重跑 → 重入后 VisitorSystem 持旧 state。
  - **H2/H3 High**：太阳 Sprite3D 未广告牌化（细条）+ 着色器未 `fog_disabled`（红黄被洗白）。
  - **H4 High**：`HorizonFogDensity=0.00025` 过低，地图外桌缘硬边重现 → 建议改 `FogMode=Depth`+`FogDepthEnd≈FarClip`。
  - 一批 Medium（死存档 API、v24 迁移、LMDB 32MB 上限、M5/M16 魔法数字、M8 `half` GLSL 保留字、M11 ScrollBackdrop alpha、M17 道路 Uv1Scale/Repeat、M18 StampCenter 半格偏移等）与 Low（魔法数字批）。
- 健康项：10 个已知 Godot 4.7 坑在 UI 层全规避、未复现黑太阳；互市闭环无 NPE；无 NuGet/GDScript、注释率达标。

## 已修复（第一批：加载/重入生命周期）
- **C1**：`GameState.RegisterRoadCell`/`RegisterEdgeCell` 加 `raiseEdgeEvent` 开关；`SaveService.LoadData` 建路循环改 `gs.RegisterRoadCell(c, false)` 静默，避免后台线程广播 `RoadReachedEdge` 跨线程碰 HUD。`SetZone` 不广播事件已确认无此风险。
- **H1**：`Main` 读档应用处（GameState.I=gs 后、EnterWorld 之后）加 `_visitors?.Setup(_clock, gs)` 重绑访客系统到新 state（F9/游戏内读档时 EnterWorld 提前 return 不再重跑 Setup）。
- **H5**：`ForeignVisitor` dwell 默认改为有限值（DwellSecondsMin..Max），仅在 `VisitorSystem.OnVisitorArrived` 分配到摊位时调新增 `SetDwellPermanent()` 置 `float.MaxValue`；无摊位小贩因此可到点自动离场，修复访客系统软锁。
- `dotnet build -p:UseSharedCompilation=false` → 0 警告 0 错误。
- 待实跑验证：读档（尤其边缘有路存档、F9 游戏内读档）不再卡死/异常；访客不再泄漏、上限 60 能持续刷新。
